{
  "name": "flow-ruffle-player",
  "version": "1.0.0",
  "private": true,
  "description": "Run the original 2006 browser Flash game flOw via Ruffle",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:pages": "GITHUB_PAGES=true vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.3.5",
    "vite-plugin-static-copy": "^2.3.1"
  }
}
